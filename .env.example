# Base URL of the TeleMed backend API (include the /api suffix).
VITE_API_URL=http://localhost:5000/api

# Base URL for the Socket.io connection (chat + video signaling + presence).
# This is the backend's root origin WITHOUT the /api suffix.
VITE_SOCKET_URL=http://localhost:5000
